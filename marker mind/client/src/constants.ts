/**
 * Application-wide constants
 * Centralized configuration values for consistency and maintainability
 */

// Sticky Note Constraints
export const MIN_NOTE_SIZE = 60;
export const DEFAULT_NOTE_WIDTH = 200;
export const DEFAULT_NOTE_HEIGHT = 200;

// Zoom Constraints
export const MIN_ZOOM = 0.25;
export const MAX_ZOOM = 3.0;

// History Management
export const MAX_HISTORY_SIZE = 20;

// Line Tool
export const LINE_HIT_AREA_WIDTH = 24; // Transparent hit area for easier clicking

// UI Timing
export const AUTO_FOCUS_DELAY_MS = 100; // Delay before clearing newly created element tracking
